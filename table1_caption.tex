\caption{The compute layout for each simulation suite.  Shown is the number of nodes, $N_N$; the
number of cores, $N_C$; the number of fields per zone, $N_F$; the number of
datasets stored per simulation, $N_D$; and the
performance in (zone-update)/(core-second), $\zeta$.}
